/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [],
    },
    // Useful for static export if needed later
    // output: 'export',
};

module.exports = nextConfig;
